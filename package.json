{
  "name": "stellar-trustline-manager",
  "version": "1.0.0",
  "description": "A tool to manage Stellar trustlines",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c green,cyan \"PORT=3000 node server.js\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.3.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "file-saver": "^2.0.5",
    "flag-icons": "^7.5.0",
    "jspdf": "^3.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
